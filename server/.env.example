# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# mDNS Hostname
MDNS_HOSTNAME=llm-arena.local

# Database
DATABASE_URL="file:./dev.db"

# CORS
CORS_ORIGIN=*

# Rate Limiting (100 is too low — telao polling alone hits ~130 req/min from one IP)
RATE_LIMIT_MAX=500
RATE_LIMIT_TIME_WINDOW=60000

# WebSocket
WS_COMPRESSION=false
WS_MAX_PAYLOAD=1048576

# Session
PIN_LENGTH=6
SESSION_TIMEOUT_MS=7200000
